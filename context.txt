# Service Desk — технический долг (июль 2026)

## Текущее состояние

- **Фронт**: 71% stmts, 61% branch, 61% funcs, 74% lines — 366 тестов (51 suite)
- **Сервер**: 71% stmts, 64% branch, 72% funcs, 73% lines — 346 тестов (25 suites)
- **E2E**: 14 Playwright spec'ов
- **ESLint**: 0 errors, 0 warnings

## Ключевые файлы

### Сервер (server/src/)
| Файл | Coverage | Статус |
|------|----------|--------|
| routes/tickets.js | ~75% | ✅ Основные кейсы |
| routes/auth.js | ~73% | ✅ |
| routes/admin.js | ~79% | ✅ |
| routes/notifications.js | ~73% | ⚠️ Нужны тесты ошибок |
| routes/push.js | ~66% | ⚠️ VAPID send path |
| routes/search.js | 100% | ✅ |
| routes/calendar.js | ~66% | ⚠️ |
| services/tickets.service.js | ~91% | ✅ |
| socket.js | ~22% | ❌ WS трудно тестировать |
| background.js | ~50% | ❌ Bull не внедрён |
| api.test.js | n/a | 145 интеграционных тестов |

### Клиент (src/)
| Файл | Coverage | Статус |
|------|----------|--------|
| Dashboard.tsx | 100% | ✅ |
| Admin.tsx | 98% | ✅ |
| Tickets.tsx | 90% | ✅ |
| Calendar.tsx | 84% | ✅ |
| Files.tsx | 84% | ✅ |
| News.tsx | 80% | ✅ |
| Polls.tsx | 74% | ✅ |
| Employees.tsx | 56% | ⚠️ Табличный вид |
| ChatDetail.tsx | 51% | ⚠️ Socket сложно |
| TicketDetail.tsx | 51% | ⚠️ Статусы/логика |
| Kanban.tsx | 47% | ⚠️ DnD |
| ticket-context.tsx | 35% | ❌ Context + Query |
| Search.tsx | ~60% | ⚠️ |

### Инфраструктура (0% покрытия — не тестируется)
- index.js (entry point)
- middleware/fileCheck.js
- middleware/metrics.js
- db.js (пул)

## Что осталось

### Высокий приоритет
1. **Bundle size**: index chunk 1.2MB — React.lazy + code splitting по страницам
2. **Bull Queue** для background.js — ретраи SLA-check + cleanup вместо setInterval
3. **N+1 аудит**: проверить все Prisma запросы на include/_count

### Средний приоритет
4. **Feature Flags** — server route + context для включения фич без деплоя
5. **API Versioning** — `/api/v1/` префикс для безопасных breaking changes
6. **a11y**: `prefers-reduced-motion` для вестибулярных
7. **Индексы**: проверить FULLTEXT на search, composite на частые WHERE

### Низкий приоритет
8. Socket.js тесты (~22%)
9. ticket-context.tsx тесты (~35%)
10. Visual regression (Chromatic)

## Команды
```bash
# Пути (Windows)
cd E:\Tickets cursor\ticketscursor
npm run test                    # фронт
npx vitest run --coverage       # coverage фронт
cd server && npm test           # сервер
npx vitest run --coverage       # coverage сервер
npm run build                   # production сборка
```

## Аудит по Промты.txt

Из `Промты.txt` (изначально под SwiftMatch) в проект внедрено:
- ✅ Outbox Pattern (events)
- ✅ Idempotency Keys
- ✅ Soft Deletes
- ✅ Graceful Shutdown
- ✅ Health check + Readiness probe
- ✅ CSP (Helmet strict)
- ✅ Audit log
- ✅ Load testing (k6)
- ✅ Grafana dashboard
- ✅ Bundle size check в CI

Не реализовано:
- ❌ Bull Queue (background jobs)
- ❌ Feature Flags
- ❌ API Versioning
- ❌ Visual regression testing
